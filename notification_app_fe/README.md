# Notification App (FE)

Minimal Vite + React + TypeScript front-end for campus notifications.

To run (install dependencies first):

```bash
cd notification_app_fe
npm install
npm run dev
```

By default the API base is `http://4.224.186.213`; set `VITE_TEST_SERVER_URL` to override.
