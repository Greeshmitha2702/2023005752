# Notification App Backend (placeholder)

This project uses a remote evaluation service at `http://4.224.186.213` for auth, logs and notifications.

Per requirements, no backend or database is required for the submission. This folder is intentionally empty/placeholder.

If you need a local mock backend later, we can add a small Express or json-server implementation here.
